# Contributing

1. Branch from `main`
2. Keep changes focused
3. Run `npm run lint && npx tsc --noEmit && npm test && npm run build`
4. Do not commit `.env` or secrets
5. Prefer organization-scoped queries and shared services over duplicated checks
