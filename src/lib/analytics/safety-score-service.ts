/**
 * SafetyScoreService — Configured Safety Performance Score (0–100).
 * Not an objective measure of campus safety.
 */
export { calculateSafetyScore as SafetyScoreService } from "@/lib/analytics/analytics-service";
export { calculateSafetyScore } from "@/lib/analytics/analytics-service";
